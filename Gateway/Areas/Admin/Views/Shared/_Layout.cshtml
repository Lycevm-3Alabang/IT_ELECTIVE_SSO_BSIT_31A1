<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - IT ELECTIVE SSO Gateway</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style type="text/css">
        <!--
        body {
            background-color: #FAF8F5;
            color: #2D2535;
            font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
            overflow-x: hidden;
        }

        #sidebar {
            width: 260px;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1040;
            background-color: #382943;
            transition: transform 0.3s ease-in-out;
        }

            #sidebar .nav-link {
                color: #d1c9d7;
                padding: 0.85rem 1.5rem;
                font-weight: 500;
                transition: all 0.2s ease;
            }

                #sidebar .nav-link:hover,
                #sidebar .nav-link.active {
                    color: #ffffff;
                    background-color: #4a3858;
                }

        #main-wrapper {
            margin-left: 260px;
            min-height: 100vh;
            transition: margin-left 0.3s ease-in-out;
        }

        @media (max-width: 991px) {
            #sidebar

        {
            transform: translateX(-100%);
        }

        #sidebar.show {
            transform: translateX(0);
        }

        #main-wrapper {
            margin-left: 0;
        }

        .sidebar-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1030;
        }

            .sidebar-overlay.show {
                display: block;
            }

        }

        .card-custom {
            background-color: #ffffff;
            border: 0;
            border-radius: 0.75rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.02);
        }

        .stat-icon-circle {
            width: 52px;
            height: 52px;
            border-radius: 50%;
            background-color: #382943;
            color: #ffffff;
            flex-shrink: 0;
        }

        .icon-circle-sm {
            width: 38px;
            height: 38px;
            border-radius: 50%;
            background-color: #f1ecf4;
            color: #382943;
            flex-shrink: 0;
        }

        .btn-logout {
            background: none;
            border: none;
            padding: 0;
            width: 100%;
            text-align: left;
        }
        -->
    </style>
</head>
<body>
    <div id="sidebarOverlay" class="sidebar-overlay"></div>

    <div id="sidebar" class="d-flex flex-column justify-content-between text-white">
        <div>
            <div class="p-4 d-flex align-items-center justify-content-between">
                <div class="d-flex align-items-center gap-3">
                    <div class="bg-white rounded" style="width: 38px; height: 38px;"></div>
                    <div>
                        <h6 class="mb-0 fw-bold lh-1 text-white">IT ELECTIVE</h6>
                        <small class="text-white-50 fs-7">SSO Gateway</small>
                    </div>
                </div>
                <button type="button" class="btn text-white d-lg-none p-0 fs-5" id="sidebarCloseBtn">
                    <i class="fa-solid fa-xmark"></i>
                </button>
            </div>

            <ul class="nav flex-column mt-2">
                <li class="nav-item">
                    <a asp-area="Admin" asp-controller="Dashboard" asp-action="Index" class="nav-link d-flex align-items-center gap-3">
                        <i class="fa-solid fa-chart-simple"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-area="Admin" asp-controller="Users" asp-action="Index" class="nav-link d-flex align-items-center gap-3">
                        <i class="fa-regular fa-user"></i> Users
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-area="Admin" asp-controller="Groups" asp-action="Index" class="nav-link d-flex align-items-center gap-3">
                        <i class="fa-solid fa-user-group"></i> Groups
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-area="Admin" asp-controller="Applications" asp-action="Index" class="nav-link d-flex align-items-center gap-3">
                        <i class="fa-regular fa-mobile"></i> Applications
                    </a>
                </li>
                <li class="nav-item">
                    <a asp-area="Admin" asp-controller="AuditLogs" asp-action="Index" class="nav-link d-flex align-items-center gap-3">
                        <i class="fa-regular fa-file-lines"></i> Audit Logs
                    </a>
                </li>
            </ul>
        </div>

        <!-- Sidebar Bottom Logout -->
        <div class="p-3">
            <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutFormSidebar">
                <button type="submit" class="nav-link text-white-50 d-flex align-items-center gap-2 btn-logout">
                    <i class="fa-solid fa-arrow-right-from-bracket"></i> Logout
                </button>
            </form>
        </div>
    </div>

    <div id="main-wrapper">
        <header class="bg-white border-bottom px-3 px-md-4 py-3 d-flex justify-content-between align-items-center">
            <div class="d-flex align-items-center gap-3">
                <button class="btn btn-light d-lg-none border-0 p-2" id="sidebarToggleBtn" type="button">
                    <i class="fa-solid fa-bars fs-5"></i>
                </button>
                <h5 class="mb-0 fw-bold tracking-tight">DASHBOARD</h5>
            </div>

            <!-- Header Profile Dropdown -->
            <div class="dropdown">
                <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle text-dark" data-bs-toggle="dropdown">
                    <div class="bg-light rounded-circle p-2 me-2 d-flex align-items-center justify-content-center" style="width: 36px; height: 36px;">
                        <i class="fa-regular fa-user text-secondary"></i>
                    </div>
                    <div class="text-start me-2 d-none d-sm-block">
                        <div class="fw-bold fs-7 lh-1">Admin User</div>
                        <small class="text-muted fs-8">Super Administrator</small>
                    </div>
                </a>
                <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0">
                    <li><a class="dropdown-item" href="#">Profile</a></li>
                    <li><a class="dropdown-item" href="#">Settings</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                        <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post">
                            <button type="submit" class="dropdown-item text-danger d-flex align-items-center gap-2">
                                <i class="fa-solid fa-arrow-right-from-bracket"></i> Logout
                            </button>
                        </form>
                    </li>
                </ul>
            </div>
        </header>

        <main class="p-3 p-md-4">
            @RenderBody()
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        const sidebar = document.getElementById('sidebar');
        const sidebarOverlay = document.getElementById('sidebarOverlay');
        const toggleBtn = document.getElementById('sidebarToggleBtn');
        const closeBtn = document.getElementById('sidebarCloseBtn');

        function toggleSidebar() {
            sidebar.classList.toggle('show');
            sidebarOverlay.classList.toggle('show');
        }

        if (toggleBtn) toggleBtn.addEventListener('click', toggleSidebar);
        if (closeBtn) closeBtn.addEventListener('click', toggleSidebar);
        if (sidebarOverlay) sidebarOverlay.addEventListener('click', toggleSidebar);
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>